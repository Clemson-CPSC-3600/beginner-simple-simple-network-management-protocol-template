[pytest]
markers =
    bundle(level): mark test as belonging to a specific grading bundle (C, B, or A)
    points(value): mark test with point value for scoring
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*